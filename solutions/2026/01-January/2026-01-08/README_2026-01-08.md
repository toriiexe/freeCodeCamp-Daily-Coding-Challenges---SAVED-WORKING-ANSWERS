# Sorted Array?

## Summary
Determine whether an array of numbers is sorted in ascending order, descending order, or neither. Return `"Ascending"` for lowest-to-highest order, `"Descending"` for highest-to-lowest order, or `"Not sorted"` when the array follows neither order.