# Markdown Unordered List Parser

## Summary
Convert a valid Markdown unordered list into an HTML unordered list by wrapping each list item in `<li>` tags and wrapping the complete collection of items in a `<ul>` tag.