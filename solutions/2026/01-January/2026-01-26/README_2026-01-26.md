# FizzBuzz Mini

## Summary
Return a string representation of a number following the FizzBuzz rules: numbers divisible by 3 become `"Fizz"`, numbers divisible by 5 become `"Buzz"`, numbers divisible by both become `"FizzBuzz"`, and all other numbers are returned as strings.