# Markdown Link Parser

## Summary
Convert a Markdown-formatted link into its equivalent HTML anchor tag by extracting the link text and URL, then placing them into an `<a>` element with the URL as the `href` attribute.