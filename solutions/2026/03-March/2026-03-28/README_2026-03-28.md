# Pascal's Triangle Row

## Summary
Return the specified row of Pascal's Triangle as an array, where each row starts and ends with `1` and every inner value is calculated by adding the two values directly above it from the previous row.