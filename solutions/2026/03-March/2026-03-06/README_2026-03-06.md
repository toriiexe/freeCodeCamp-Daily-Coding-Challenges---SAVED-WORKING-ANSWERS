# Trail Traversal

## Summary
Follow the single continuous trail from the current location (`C`) to the goal (`G`) and return the sequence of moves needed to reach it, using `R`, `D`, `L`, and `U` to represent each step.