# Element Size

## Summary
Calculate an element's width and height in pixels based on the browser window size and its dimensions specified in `vw` and `vh` units. Return the resulting size in the format `"width x height"`.