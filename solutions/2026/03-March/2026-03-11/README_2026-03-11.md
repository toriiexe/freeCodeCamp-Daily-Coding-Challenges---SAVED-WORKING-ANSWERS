# Word Length Converter

## Summary
Convert a string of space-separated words into a new string where each word is replaced by its character length, preserving the spaces between the resulting numbers.