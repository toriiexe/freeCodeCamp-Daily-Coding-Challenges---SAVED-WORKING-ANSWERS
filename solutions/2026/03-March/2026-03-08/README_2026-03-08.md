# HSL Validator

## Summary
Validate whether a string is a correctly formatted CSS `hsl()` color value by checking the syntax, allowed spacing, optional semicolon, and ensuring hue, saturation, and lightness values fall within their valid ranges.