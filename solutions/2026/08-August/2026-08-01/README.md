# Magic Square Solver

## Summary
Determine the missing value in a 3×3 magic square by replacing the `0` with the number that makes every row, column, and diagonal sum to the same total. Return the missing number if a valid magic square can be completed; otherwise, return `"impossible"`.