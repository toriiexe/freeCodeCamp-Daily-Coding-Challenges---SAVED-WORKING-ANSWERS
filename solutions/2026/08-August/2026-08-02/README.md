# Food Chain

## Summary
Construct the food chain by identifying the apex predator—the animal that is never listed as prey—and following each predator-to-prey relationship down to the final animal. Return the ordered chain as an array of strings.