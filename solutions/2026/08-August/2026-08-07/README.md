# Nonogram Validator

## Summary
Determine whether a row of filled (`1`) and empty (`0`) cells matches a given nonogram clue. The clue specifies the lengths of consecutive groups of filled cells, in order, with at least one empty cell separating adjacent groups.