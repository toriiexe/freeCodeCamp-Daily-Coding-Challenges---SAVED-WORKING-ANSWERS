# Wider Aspect Ratio

## Summary
Compare the width-to-height ratios of two image dimensions and return the simplified aspect ratio of the image with the greater ratio. The input dimensions are provided in the format `WxH`, and the result should be returned as `W:H`.