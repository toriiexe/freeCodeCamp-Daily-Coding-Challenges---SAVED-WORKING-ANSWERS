# Deepest Brackets

## Summary
Extract and return the content inside the deepest level of nested brackets in a balanced bracket string. The brackets may be `()`, `[]`, or `{}`, and the input is guaranteed to have a single deepest nested group.