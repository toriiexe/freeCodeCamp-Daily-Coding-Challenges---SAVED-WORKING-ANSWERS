# I Before E

## Summary
Correct words in a given string so they follow the "I before E except after C" rule. Replace occurrences of `"ei"` with `"ie"` when not preceded by `"c"`, and replace `"ie"` with `"ei"` when it is preceded by `"c"`. All other words remain unchanged.