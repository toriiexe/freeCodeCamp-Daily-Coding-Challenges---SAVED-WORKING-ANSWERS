# Oldest Person

## Summary
Find the person(/people) with the highest age in a list of objects containing `name` and `age` properties. Return an array of names of the oldest person or people, preserving their original order if there is a tie.