# Truncate the Text

## Summary
Return the original string if it is 20 characters or fewer. Otherwise, shorten it to the first 17 characters and append `...` so the returned string is exactly 20 characters long.