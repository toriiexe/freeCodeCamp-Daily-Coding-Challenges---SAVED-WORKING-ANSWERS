# Matrix Shift

## Summary
Shift all values in a matrix left or right by the specified amount, treating the matrix as one continuous sequence with wraparound, and return the resulting matrix with its original dimensions preserved.