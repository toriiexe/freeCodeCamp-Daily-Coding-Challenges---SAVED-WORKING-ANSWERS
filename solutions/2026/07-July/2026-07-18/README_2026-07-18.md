# Dice Odds

## Summary
Calculate the odds of rolling a specific target sum with a given number of six-sided dice. Return the probability as `"1 in X"`, where `X` is the reciprocal of the probability rounded to the nearest whole number.