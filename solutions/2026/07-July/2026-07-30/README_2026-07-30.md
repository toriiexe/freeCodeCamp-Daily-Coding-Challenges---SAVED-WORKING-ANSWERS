# Contrast Rating 3

## Summary
Calculate the WCAG contrast rating from two RGB colors by converting each color to relative luminance, computing the contrast ratio, and returning `"AAA"`, `"AA"`, or `"Fail"` based on whether the text is normal or large.