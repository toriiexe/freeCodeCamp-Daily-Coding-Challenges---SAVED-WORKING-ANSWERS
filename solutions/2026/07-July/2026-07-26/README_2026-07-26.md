# Letter Distance

## Summary
Calculate the total shortest circular distance between corresponding characters in two equal-length strings. The alphabet wraps around like a circle, so the distance between `"a"` and `"z"` is 1.