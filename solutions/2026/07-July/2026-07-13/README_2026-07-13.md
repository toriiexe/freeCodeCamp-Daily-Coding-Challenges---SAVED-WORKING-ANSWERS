# Tally Counter

## Summary
Calculate the total value represented by a string of tally marks, where each `|` counts as one and each `/` completes a group of five (`||||/`). Ignore spaces separating tally groups.