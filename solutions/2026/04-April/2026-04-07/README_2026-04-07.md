# Palindrome Characters

## Summary
Determine whether a string is a palindrome. If it is, return its middle character for odd-length strings or its middle two characters for even-length strings; otherwise, return `"none"`.