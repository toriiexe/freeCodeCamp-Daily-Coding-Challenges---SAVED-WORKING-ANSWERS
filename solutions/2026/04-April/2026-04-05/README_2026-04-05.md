# Digit Rotation Escape

## Summary
Check a number and its rotations to find the first version that is evenly divisible by the number of digits it contains. Start with the original number before applying any rotations, and return the first valid rotation or `"none"` if no rotation works.