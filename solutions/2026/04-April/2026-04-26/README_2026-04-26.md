# FizzBuzz Explosion

## Summary
Calculate how many transformation steps are needed for the string `"fizzbuzz"` to contain at least the given number of `"z"` characters. In each step, replace every character according to its 1-based position using the FizzBuzz rules, then count the resulting `"z"` characters.