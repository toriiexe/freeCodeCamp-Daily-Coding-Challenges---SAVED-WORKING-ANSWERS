# Closest Time Direction

## Summary
Determine the shorter direction to move between two times on a 24-hour clock that wraps around. Return `"forward"` if moving forward takes less time, `"backward"` if moving backward takes less time, or `"equal"` if both directions require the same amount of time.