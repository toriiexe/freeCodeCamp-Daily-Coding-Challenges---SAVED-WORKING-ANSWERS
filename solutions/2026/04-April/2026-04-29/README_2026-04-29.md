# URL Query Parser

## Summary
Parse the query string from a URL into a dictionary of key-value pairs by extracting everything after the `?`, splitting parameters on `&`, and separating each key and value at `=`. Return all values as strings.