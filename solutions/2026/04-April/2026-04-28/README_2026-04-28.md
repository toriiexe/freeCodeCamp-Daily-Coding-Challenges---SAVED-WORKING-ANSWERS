# Number Words

## Summary
Convert an integer from `0` to `99` into its English word representation, using unique names for `0–19`, the standard names for multiples of ten, and hyphenated words for other two-digit numbers.