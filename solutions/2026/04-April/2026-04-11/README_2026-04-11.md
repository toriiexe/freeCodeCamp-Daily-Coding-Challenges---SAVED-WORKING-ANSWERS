# Rook and Bishop Attack

## Summary
Determine whether a rook or bishop can attack the other piece on an 8x8 chessboard. Return `"rook"` if the rook attacks the bishop, `"bishop"` if the bishop attacks the rook, or `"neither"` if neither piece can reach the other's position.