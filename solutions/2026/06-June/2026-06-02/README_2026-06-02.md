# Schema Validator Part 2

## Summary
Validate whether an object or dictionary follows a given schema by checking that the required properties exist and have the correct data types. Additional properties are allowed.