# Schema Validator Part 3

## Summary
Validate whether an object or dictionary follows a given schema by checking that required properties exist and have the correct data types, including whether the role value matches one of the allowed options. Additional properties are allowed.