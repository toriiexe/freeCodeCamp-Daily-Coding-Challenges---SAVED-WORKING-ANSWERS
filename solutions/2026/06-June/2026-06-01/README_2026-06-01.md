# Schema Validator Part 1

## Summary
Validate whether an object or dictionary follows a given schema by checking that the required `username` property exists and contains a string value. Additional properties are allowed.