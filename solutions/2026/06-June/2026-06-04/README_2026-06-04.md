# Schema Validator Part 4

## Summary
Validate whether an object or dictionary follows a given schema by checking that required properties exist and have the correct data types, including whether the role value matches one of the allowed options. Optional properties should be validated when present, and additional properties are allowed.