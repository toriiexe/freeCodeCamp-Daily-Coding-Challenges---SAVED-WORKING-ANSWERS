# Traveling Shopper

## Summary
Convert all prices to a common currency using the given exchange rates, then determine how many items can be purchased in the given order with the available money. Return `"Buy them all!"` if every item is affordable; otherwise, return `"Buy the first X items."`, where `X` is the number of items that can be purchased.