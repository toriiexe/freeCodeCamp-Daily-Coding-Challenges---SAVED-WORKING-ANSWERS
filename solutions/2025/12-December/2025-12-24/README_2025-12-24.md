# Markdown Image Parser

## Summary
Convert a Markdown image syntax (`![alt text](image_url)`) into an HTML `<img>` tag with the `src` attribute set to the image URL and the `alt` attribute set to the image description, preserving the required attribute order and formatting.