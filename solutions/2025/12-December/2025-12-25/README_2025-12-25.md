# Snowflake Generator

## Summary
Create a horizontally mirrored version of a multi-line string by appending a reversed copy of each line to itself, preserving all spaces and line breaks exactly as they appear.