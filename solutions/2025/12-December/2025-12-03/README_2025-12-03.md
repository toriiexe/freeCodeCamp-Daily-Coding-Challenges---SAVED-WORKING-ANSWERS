# Markdown Ordered List Item Converter

## Summary
Convert a valid Markdown ordered list item into its equivalent HTML list item element. Validate the required format and return the list text wrapped in `li` tags, or indicate an invalid format when the input does not match the expected structure.