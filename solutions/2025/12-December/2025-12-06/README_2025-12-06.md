# Date Formatter

## Summary
Convert a date from the format `"Month day, year"` into the ISO format `"YYYY-MM-DD"` by translating the month name into its numeric value and padding the month and day with leading zeros when needed.