# RGB to Hex

## Summary
Convert a CSS RGB color string into its hexadecimal color representation. Format the result with a leading `#`, exactly six hexadecimal characters, and lowercase letters without using shorthand notation.