# Slug Generator

## Summary
Convert a string into a URL-friendly slug by normalizing letter casing, removing invalid characters, and replacing spaces with `%20` while avoiding duplicate, leading, or trailing encoded spaces.