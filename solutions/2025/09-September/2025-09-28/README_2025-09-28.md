# CSV Header Parser

## Summary
Parse the first line of a CSV file and return an array of column headings. Split the headings by commas and remove any leading or trailing whitespace from each heading.