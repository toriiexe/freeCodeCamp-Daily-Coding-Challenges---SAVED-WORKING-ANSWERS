# Targeted Sum

## Summary
Find two unique numbers in an array that add up to a given target value and return their indices in ascending order. If no matching pair exists, return `"Target not found"`.