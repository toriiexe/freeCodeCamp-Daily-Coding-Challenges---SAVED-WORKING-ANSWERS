# Unnatural Prime

## Summary
Determine whether a given integer is a prime number or a negative prime number. Positive primes and their negative counterparts are considered valid, while `0`, `1`, and `-1` are not prime.