# Reverse Parenthesis

## Summary
Decode a string by reversing the characters inside each pair of properly nested parentheses, processing innermost pairs first, and removing all parentheses from the final result.