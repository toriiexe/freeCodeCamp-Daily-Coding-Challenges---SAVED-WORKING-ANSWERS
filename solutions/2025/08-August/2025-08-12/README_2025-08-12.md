# Base Check

## Summary
Determine whether a string represents a valid number in the specified base by checking that every character is a valid digit for that base. The validation should support bases from 2 to 36 and be case-insensitive.