# Hex Generator

## Summary
Generate a random six-character hexadecimal color code where the requested color (`"red"`, `"green"`, or `"blue"`) has a higher value than the other color channels. Return `"Invalid color"` for unsupported color names.