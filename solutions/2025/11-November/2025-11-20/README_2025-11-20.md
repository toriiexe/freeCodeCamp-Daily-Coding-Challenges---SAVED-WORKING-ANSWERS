# Longest Word

## Summary
Find and return the longest word in a sentence based on its letter count. Ignore punctuation when determining length, preserve the original appearance of the selected word, and return the first word when multiple words share the same length.