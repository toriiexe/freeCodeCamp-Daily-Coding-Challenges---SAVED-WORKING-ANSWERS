# Markdown Heading Converter

## Summary
Convert a valid Markdown heading into its equivalent HTML heading tag by determining the heading level from the number of leading `#` characters. Return `"Invalid format"` if the input does not follow the required Markdown heading syntax.