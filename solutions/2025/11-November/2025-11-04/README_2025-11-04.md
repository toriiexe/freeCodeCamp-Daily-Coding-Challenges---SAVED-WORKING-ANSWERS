# Image Search

## Summary
Filter a list of image names by returning only those that contain a given search term. The search should be case-insensitive, and the original order of matching image names should be preserved.