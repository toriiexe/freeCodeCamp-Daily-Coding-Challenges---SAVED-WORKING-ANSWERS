# HTML Attribute Extractor

## Summary
Extract all attributes from a single valid HTML element and return them as an array of `"attribute, value"` strings in the order they appear. Return an empty array if the element has no attributes.