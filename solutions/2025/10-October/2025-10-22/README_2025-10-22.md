# Speak Wisely, You Must

## Summary
Transform a sentence into a wise-teacher style by moving all words up to and including the first occurrence of `"have"`, `"must"`, `"are"`, `"will"`, or `"can"` to the end, lowercasing the moved words, capitalizing the new first word, preserving the original ending punctuation, and separating the moved portion with a comma.