# Navigator

## Summary
Simulate a web browser's navigation history starting from the **Home** page by processing **Visit**, **Back**, and **Forward** commands. Return the name of the page displayed after all commands have been executed, correctly maintaining back and forward history.